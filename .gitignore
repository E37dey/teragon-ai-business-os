node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
!.env.deploy.example
!.env.staging.local.example
# S6.1 automated credential bootstrap output — NEVER commit (also matched by
# *.local and .env.* above; listed explicitly as a hard guard).
.env.staging.local
.teragon-staging-recovery.*
# S7.0 staging stage tracker — safe masked metadata only, still never committed.
.teragon-staging-state.json
.netlify
playwright-report
test-results
# Live-suite machine-readable reports (safe counts, but ephemeral — never committed).
ci-artifacts/
e2e/live-domains/_report.json
e2e/live-domains/_pw.json
e2e/live-domains/_idb.json
e2e/pilot/_cross-browser.json
e2e/pilot/_a11y.json
e2e/e2e/
*.tsbuildinfo
.claude/

# Supabase local/ephemeral state (config.toml + migrations + tests ARE tracked)
supabase/.temp/
supabase/.branches/
supabase/.env
.supabase/
