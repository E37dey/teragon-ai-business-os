# מדריך מרכז ההגשה (W7-E · Phases 7.15-7.19, 7.24)

23.07.2026 · בעלות: `src/domain/submission/**`, `src/validation/submission/**`,
`src/modules/submission/**`, `tests/submission/**`

## מה נבנה

| רכיב | קובץ | תמצית |
|---|---|---|
| רישום תוכן קנוני (7.24) | `src/domain/submission/contentRegistry.ts` | זהות (טרגון טכנולוגיות · צחי זוסטייהם · "ערב טוב, צחי"), שומרי ספירה 7/13/6/6/12/5, ולידטור drift מול המקורות החיים, דחיית האיות "צחי זוסטהיים" (C2), בעלים = 5 משתמשי seed בלבד (C3) |
| 3 רמות מדידה (7.16) | `src/domain/submission/metricLevels.ts` | 22 מדדים: רמה A הדרכה (5) · רמה B אימוץ (8) · רמה C עסקי (9). כל מדד: הגדרה, סוג (מחושב/יעד פיילוט/מבני), נוסחה, מקור, תקופה, קו בסיס (null ⇒ "לא הוגדר קו בסיס"), יעד נפרד, בעלים בשם, מגבלות. המחושבים מחווטים לאוספים אמיתיים: זמן תגובה לליד ← activities+leads · המרת הצעות ← quotations · זמן פתרון ← serviceTickets · שיעורי אושר/נערך/נדחה ← approvals · נפח+אסקלציה ← supportRequests · השלמת למידה ← enrollments (selector קיים) |
| תוצר תמיכה (7.15) | `src/domain/submission/supportDeliverable.ts` | 3 שכבות SPEC פרק 18 עם SLA יעד (מיידי/שעתיים/יום עבודה, C10) מופרד מ-SLA מדוד (חציון+עמידה מ-supportRequests סגורות בפועל, אחרת "טרם נמדד"); נושאים חוזרים, שגרת רענון, קישורים ל-/support /knowledge /faq |
| מודל ההגשה (7.18) | `types.ts` + `schemas.ts` + `deliverables.ts` + `readiness.ts` + `snapshot.ts` | SubmissionPackage/Deliverable/Evidence/Validation/Blocker/Snapshot מוקלדים + zod. 12 התוצרים עם resolvers של ראיות אמת. מצבים: חסר/חלקי/ממתין לבדיקה/מלא/חסום/פג תוקף. "מלא" נגזר בלבד: תוכן + בעלים בשם + אישור תקף + ראיות נפתרות + אין כשל איכות + נתיב קיים ב-APP_ROUTES + הדפסה |
| ולידטור 8 הפריטים (7.17) | `src/validation/submission/qualityValidator.ts` | בדיוק 8 קריטריונים (בהירות/רלוונטיות/פרסונליזציה/בטיחות/אחריות/תרגול/מדידה/תחזוקה), בדיקות עומק דטרמיניסטיות (לא נוכחות-טקסט): תרגול נכשל בלי תרגיל, אחריות נכשלת על תפקיד-במקום-אדם, מדידה מזהירה על יעד מספרי בלי מקור, בטיחות בודקת את רשימת ה"אסור" ב-tm-4 מול CORRECT_USE_RULES, תחזוקה בודקת reviewDate/תפוגה. אין 8/8 קשיח. שמירה ל-qualityValidations במזהים דטרמיניסטיים (qv-<key>-<n>) |
| מסך /submission (7.19) | `src/modules/submission/SubmissionPage.tsx` | כותרת "מוכנות להגשה" (נגזרת — לעולם לא ירוק עם חוסם), 12 כרטיסי תוצר, מטריצת איכות 12×8 אמיתית, לוח מדדים, תוצר תמיכה, One-Pager, פאנל docs/QA/Build כן, PageRail "מבקר ההגשה" |
| תצוגת הדפסה (7.20) | `src/modules/submission/printView.tsx` | A4 · RTL · פלטת הדפסה בהירה · מספרי עמוד ב-CSS counters · מוצר/גרסה/תאריך/בעלים/אישור/הפניות ראיה · ללא ניווט · `?print=1` · הדפסת דפדפן / שמירה כ-PDF בלבד (אין טענת PDF נטיבי) |

## עקרונות כנות שנאכפים בקוד ובבדיקות

- אף מספר דונור לא מיובא כמדידה (blanket ruling #1): ROI 3X, WAU 70%, NPS,
  ₪3,840 — קיימים רק כטקסט יעד מסומן; `measureMetric` מחזיר null לכל מה שאינו
  "מחושב".
- baseline ≡ null בכל 22 המדדים ⇒ "לא הוגדר קו בסיס". יעד מוצג בעמודה נפרדת
  ולעולם לא בעמודת "נמדד עכשיו".
- המצב ההתחלתי הכן: 0/12 "מלא" — אין רשומות אישור מהמנוע הקנוני
  (subjectRef=`submission-deliverable:<key>`), ולכן רוב התוצרים "ממתין לבדיקה"
  או "חלקי"; stage-gates/implementation-plan "חסר" עד שה-bootstraps רצים
  (המסך מריץ אותם אידמפוטנטית בעלייה).
- תפרי W7-F: `presenterNotes` ריק ⇒ "חסר — הערות מרצה טרם נוצרו (W7-F)";
  `presentationSections` נקראות לחישוב חריגת 10 הדקות.
- "מבקר ההגשה": תוצר חסר · ספירה סותרת · ספירת פרסונות שגויה (דרך
  exactly7Personas/גשר) · חוסר קו בסיס · בעלים לא-בשם · ראיה לא נפתרת · חומר
  שפג תוקפו · נתיב שבור · הערת מרצה חסרה · חריגת תזמון · צילום מסך לא קיים —
  כל ממצא פותח את היעד שלו.

## הפעלה

- המסך: `/submission` (לאחר חיווט הראוטר — ראו `docs/integration-requests-w7e.md`).
- הדפסה: `/submission?print=1` → כפתור הדפסה/שמירה כ-PDF.
- שמירת תוצאות איכות: כפתור "שמירת תוצאות הבדיקה" (upsert ל-qualityValidations).
- צילום מצב: כפתור "צילום מצב" (append ל-submissionSnapshots).

## בדיקות (tests/submission/**, 60 בדיקות)

registry drift (נכשל על כל סטייה) · ולידטור לפי קריטריון + אין-8/8-קשיח +
דטרמיניזם · מטריצת מוכנות (כל תנאי "מלא" חוסם בנפרד) · הפרדת סוגי מדד +
baseline + יעד≠מדוד · תמיכה יעד-מול-מדוד · חוסם→חישוב מוכנות מחדש · זהות בכל
התוכן שנכתב.
