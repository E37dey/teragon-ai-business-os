# INTEGRATION REQUESTS — W5-A (AI Contracts & Provider Architecture)

23.07.2026 · מבקש: W5-A · קבצים משותפים בבעלות Integration Lead בלבד — W5-A לא נגע בהם.

| #   | קובץ משותף                            | שינוי מבוקש                                                                                                                                                                                                                  | נימוק                                                                                                                                     | דחיפות                |
| --- | ------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- | --------------------- |
| 1   | `src/domain/types.ts`                 | להוסיף re-export: `export type { AIResponseEnvelopeV2, EvidenceItem, ConfidenceInfo, ApprovalInfo, UsageInfo, AIEnvelopeStatus } from "./ai/envelope";`                                                                      | צרכני domain ימשיכו לייבא הכל מ-types.ts; הטיפוסים עצמם כבר קיימים ב-`src/domain/ai/envelope.ts` (בבעלות W5-A) כך שאין חסימה — נוחות בלבד | נמוכה                 |
| 2   | `src/domain/types.ts`                 | הערת deprecation על `AIResponseEnvelope` (דור 1): להפנות ל-V2 ולגשר `toLegacyEnvelope`/`fromLegacyEnvelope`                                                                                                                  | מניעת שימוש חדש במעטפת הישנה; הישנה נשארת תקפה לרשומות `AIRecommendation`                                                                 | נמוכה                 |
| 3   | `src/app/**` (providers)              | בעת חיווט ה-UI (5.3+): יצירת `ProviderRegistry` יחיד ברמת ה-app (`remote: RemoteAIProvider`, `local: LocalRulesProvider(repositoryDataAccess())`) והזרקת ה-`RegistryConfig` מ-endpoint תצורה ציבורי של W5-B — לא מ-env בלקוח | Registry הוא singleton אפליקטיבי; W5-A מספק את המחלקות בלבד                                                                               | בינונית (Wave 5 המשך) |
| 4   | `src/design-system/ConfidenceBar.tsx` | (לא חוסם) לוודא שהרינדור הטקסטואלי עובר דרך `confidenceDisplayHe()` מ-`src/domain/ai/envelope.ts` כשמציגים `ConfidenceInfo` של V2                                                                                            | מקור אמת אחד לחוזה "טרם נמדד / לעולם לא 0"                                                                                                | נמוכה                 |
| 5   | `package.json`                        | **אין בקשה** — לא נדרשו תלויות חדשות (zod קיים)                                                                                                                                                                              | —                                                                                                                                         | —                     |
| 6   | W5-B (`netlify/functions/**`)         | לממש את 8 ה-endpoints לפי `src/ai/contracts/serverDto.ts` (v1): הד `x-correlation-id`, גוף שגיאה אחיד עם 11 הקודים, `envelope.model` מהשרת, NDJSON ב-`ai-stream`, endpoint תצורה ציבורי לדגלי `RegistryConfig`               | זהו החוזה המחייב; פירוט מלא ב-docs/AI_ARCHITECTURE.md §DTO v1                                                                             | גבוהה                 |

הערות:

- W5-A לא שינה אף קובץ מחוץ לבעלותו: `src/ai/**`, `src/domain/ai/**`, `tests/ai/contracts/**`, שני מסמכי docs אלה.
- `LocalRulesProvider` מייבא read-only פונקציות טהורות מ-`src/modules/sales/matching.ts` (מותר לפי כללי ה-Wave; אין מעגל תלויות — modules אינם מייבאים מ-src/ai).
