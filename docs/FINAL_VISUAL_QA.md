# FINAL VISUAL QA — W9-C (Phase 9.11)

תאריך: 24.07.2026 · מקור: `e2e/final-regression/w9c-visual.spec.ts`
· פלט: `docs/screenshots/final/` · **106 קבצי PNG** מריצת אמת על `dist/`.

---

## 1. היקף הצילומים

| קבוצה | כמות | פירוט |
|---|---|---|
| 31 מסלולים × 1920×1080 | 31 | `route-*-1920x1080.png` |
| 31 מסלולים × 2560×1440 | 31 | `route-*-2560x1440.png` |
| 31 מסלולים × 3840×2160 | 31 | `route-*-3840x2160.png` |
| מצבי מפתח @1920×1080 | 10 | `state-*.png` |
| תצוגות הדפסה A4 (794×1123) | 3 | `print-*.png` |
| **סה"כ** | **106** | |

בדיקות הצילום: **103/103 עברו** (3 צילומי ההדפסה נוצרים בחבילת 9.12).
אין קובץ ריק/פגום — נבדק: 0 קבצים מתחת ל-2KB.

כל צילום ממתין לסמן התוכן האמיתי של המסך לפני הלכידה, ולכן **אף צילום אינו
מציג stub של טעינה** (`.os-route-loading` מאומת כנפתר).

### 10 מצבי המפתח שנלכדו
`state-notifications-drawer` · `state-command-palette` · `state-global-search` ·
`state-quick-create-lead` · `state-submission-pending-approval` ·
`state-system-health-degraded` · `state-settings-ai-provider-disabled` ·
`state-presentation-present-mode` · `state-presentation-examiner` ·
`state-print-submission-a4`

---

## 2. רשימת הבדיקה של הבריף — תוצאות

| בדיקה | תוצאה | ראיה |
|---|---|---|
| ניווט בצד **ימין** | ✅ | סרגל TERAGON בימין בכל מסכי ה-shell |
| לוח הקשר (rail) בצד **שמאל** | ✅ | `complementary "לוח הקשר"` בשמאל |
| פריסת RTL | ✅ | `dir=rtl`; טקסט עברי מיושר לימין; חצים "←" בכיוון הקריאה |
| מספרים/מטבע ב-LTR | ✅ | `13,034 ₪` · `100,990 ₪` · `14%` · `24.07.2026` — ספרות בכיוון LTR תקין |
| צפיפות (density) | ✅ | עקבית בשלוש הרזולוציות; אין דחיסה ב-3840×2160 |
| אין חיתוך / גלישה | ✅ | לא נצפתה גלישה אופקית או טקסט חתוך באף רזולוציה |
| אין "ירוק מזויף" בבריאות | ✅ | `/system-health` מציג **"לא זמין"** ל-netlify-functions ב-preview; מרכז הפיקוד מציג **"טרם נבדק"** / **"טרם נמדד"** במקום ירוק |
| אין טקסט placeholder | ✅ | אין `lorem` / `FIXME` בבנדל; מופעי `placeholder` הם תכונת `placeholder=` של שדות קלט בלבד |
| אין שם מוצר legacy ישן | ✅ | סריקת `dist/assets/*.js`: אין `teragon-final` / `hermes` / `supabase` |
| אין נתיב מכונה מקומי גלוי | ✅ | אין `C:\Users` / `/Users/…` בבנדל או במסכים |

---

## 3. מחזורי תיקון

**מחזורים שנוצלו: 2 מתוך 3 המותרים.**

| מחזור | ממצא | פעולה |
|---|---|---|
| 1 | קישורי "חדר התיאום" במרכז הפיקוד מובחנים בצבע בלבד (axe serious `link-in-text-block`) | הוספת קו תחתון; build מחדש; הצילומים נלכדו מה-build המתוקן |
| 2 | שבב `.os-palette__kbd` בניגודיות 4.67:1 — על סף AA (axe serious `color-contrast`), נתפס רק בריצה המלאה | החלפת טוקן ל-`--os-text-2` (7.07:1); בנוסף נוסף `settleOverlays()` כדי שכל צילום/מדידה יתבצעו **אחרי** סיום אנימציית הכניסה |

מחזור 3 לא נדרש: הריצה המלאה השלישית על build נקי הניבה **196/196** ללא ממצא חדש.

**שיפור איכות הצילומים ממחזור 2:** כל 106 הצילומים נלכדים כעת אחרי
`settleOverlays()` — כלומר אף צילום אינו תופס מודל/מגירה/פלטה באמצע אנימציית
הכניסה (שקיפות חלקית). זה הופך את הצילומים לדטרמיניסטיים ולנאמנים למה שהמשתמש רואה.

---

## 4. אימות ויזואלי ממוקד (בדיקת עין על צילומים נבחרים)

**`route-00-command-center-1920x1080.png`** — ניווט ימין, rail שמאל, KPI בעברית עם
מספרים LTR, "מרכז ההחלטות של ה-AI" עם מעטפת כנה ("טרם נמדד", "מצב הדגמה מקומי ·
נתוני הדגמה"), רצועת הניהול עם `0`/`22`/`12` אמיתיים ו-**"טרם נבדק"** — אין ירוק מזויף.
הקישור "להרצת תרחיש בחדר התיאום ←" מופיע **עם קו תחתון** (תיקון מחזור 1).

**`state-presentation-present-mode-1920x1080.png`** — מסך מלא אמיתי ללא chrome של
האפליקציה, מונה שקפים `1 / 5`, טיימר `10:00`, יעד שקף `2:00 / 0:00`, פקדי הערות/דמו/
גיבוי/יציאה, ורמז מקשי RTL בתחתית. תוכן אמיתי, ללא placeholder.

**`print-submission-a4.png`** — רקע לבן (ללא בזבוז דיו), RTL, מספרי עמודים
("עמוד 1"…), בעלים/גרסה/תאריך/מוצר לכל תוצר, ללא ניווט, ו-**"אישור: אין רשומת
אישור · מצב: ממתין לבדיקה"** — התוצרים אינם מאושרים אוטומטית.

---

## 5. מגבלות מתועדות

* הצילומים נלכדו ב-Chromium (Desktop Chrome) בלבד — קונבנציית המאגר לכל גלי ה-QA.
* `viewport` בלבד (לא `fullPage`), כדי לשקף את מה שהבוחן רואה בפועל במסך.
* `/system-health` מציג "לא זמין" ל-netlify-functions — זהו המצב **האמיתי** תחת
  `vite preview` (אין פונקציות), ולא תקלה. זו בדיוק הסיבה שהמצב נלכד כ-
  `state-system-health-degraded`.
