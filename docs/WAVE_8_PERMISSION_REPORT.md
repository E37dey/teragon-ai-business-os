# WAVE 8 — PERMISSION REPORT (W8-C)

תאריך: 23.07.2026 · Workstream: ADMINISTRATION, USERS AND PERMISSIONS

## מה נבנה

1. **Phase 8.0** — מלאי הרשאות אמיתי: `docs/WAVE_8_PERMISSION_INVENTORY.md` (משתמשים/תפקידים/שימוש בפועל/פערים, מבוסס בדיקת קוד).
2. **Phase 8.7 (דומיין)** — `src/domain/administration/**`: 9 תפקידים קנוניים עם מטריצת הרשאות טיפוסית (13 תחומים × 4 רמות), zod לכל רשומה, כללי שילוב אסור נאכפים קונסטרוקטיבית ב-3 שכבות, `HumanUserId` branded type + חסימת ag-* בכל נקודה.
3. **Phase 8.8 (מסך)** — `/administration` עם 8 לשוניות (משתמשים/תפקידים/הרשאות/ארגונים/סקירת גישה/בקשות שינוי/מצב חירום/Audit), PageRail עם ממתינים/מצבי חירום/אזהרות שילוב, KPI מנתוני אמת, מכבד את מצב ההדגמה לבוחן.

## ערבויות שנבדקו (tests/administration — 63 בדיקות)

| ערובה | בדיקה |
|--------|--------|
| כל 4 השילובים האסורים חסומים (פונקציה+סכמה+שירות) | guards.test.ts + changeRequest.test.ts |
| אישור עצמי חסום (בקשה + הכרעה) | guards + changeRequest |
| סוכן AI לעולם לא מאשר/נושא תפקיד/מפעיל חירום/סוקר | כל הקבצים |
| מחזור חיים מלא: preview→approve→execute→**verify בקריאה חוזרת**→audit | changeRequest.test.ts |
| דחייה = אפס מוטציה | changeRequest.test.ts |
| רק המאשר בשם מכריע; בקשה מוכרעת לא מוכרעת שוב | changeRequest.test.ts |
| גשר baseline אידמפוטנטי (ריצה שנייה = 0 יצירות) | bridge.test.ts |
| בדיוק 9 תפקידים (חסר/עודף/כפול נזרק) | guards + bridge |
| בקרות חירום: רשומה+דגל+audit, ביטול, כשל כן על יעד חסר | emergency.test.ts |
| נעילת הרשאות / קריאה בלבד נאכפות בשירות | changeRequest + emergency |
| עמוד: גשר על mount, 8 לשוניות, מטריצת 9, 5 בקרות חירום | page.test.tsx |

## שערי איכות (ריצות אמת בעת הסגירה)

ראו פלט בסיכום ה-commit: oxlint 0/0 · tsc -b 0 · typecheck:tests 0 · Vitest 1342+63=1405 · build ✓.

## מגבלות כנות

- שינוי הרשאות בפועל משנה רשומות מודל הניהול בלבד — שאר המודולים אינם אוכפים עדיין את המטריצה בקריאות שלהם (אין RBAC כלל-מערכתי ב-Wave 8); זו בקשת אינטגרציה מפורשת.
- "השבתת כל ה-AI המרוחק" היא תצוגה בלבד במצב A (אין ספק מרוחק פעיל) — מוצהר במסך וברשומה.
- דגלי automation-disable / read-only נכתבים ונאכפים במודול הניהול; אכיפה במודולים אחרים = בקשות חיווט בתור.
- סשנים סינתטיים (הסשן הנוכחי בלבד) — אין ניהול סשנים אמיתי במצב הדגמה מקומי.
