# Teragon — Session Resume (lean protocol)

> Read THIS file + only the directly-relevant source files. Do **not** reconstruct
> the full S7/S8/S9 history or re-verify frozen facts unless a command contradicts them.

## Current position
- **Branch:** `feature/teragon-supabase-app-auth` @ `26ec4ef`. **PR #3 and PR #5 are both MERGED** — #3 via `0159e97` (source `722824c`), #5 via `26ec4ef6253464893d4b69339356827fcf15045d` (source `1d446610b191e09157c756d70aab80bd20c0e8d0`, both gates green). Source branches `feature/teragon-supabase-domain-integration` and `feature/teragon-supabase-contacts-domain` are preserved. `main` is untouched at `4d4c9dc`.
- **S10.0 operational hardening MERGED** (PR **#6** via `67693a768776662f22d5eeb60744b5c0387c5cd2`, source `88d341a`; base now `67693a7`). Ships: provider-neutral error sink with a strict runtime whitelist (`kind`/`code`/`domain`/`route`/`correlationId`/`timestamp`), a root `RootErrorBoundary`, domain read/write failure + RLS-denial events with per-operation correlation ids, and two read-only Supabase/RLS health probes registered in the canonical `ALL_CHECKS` runner. No vendor SDK, no package installed. Source branch `feature/teragon-operational-hardening` preserved at `88d341a`.
  - **S10.0-F MERGED (PR #7, `af787cb`)** — the CI gap below is CLOSED. `supabase-live-validation.yml` now triggers on `pull_request.branches: [feature/teragon-supabase-app-auth]` with **no path filter**; `static-gate` is unconditional and a native `git diff` `changes` job gates only `live-database`. Confirmed live on PR #8 (docs-only): static gate **passed**, live database **skipped**.
  - **S10.1-A MERGED (PR #8, `afbd25d3cd0ecb74942bbf77f73229717bfafb6c`)** — `docs/operations/RESTORE_DRILL_RUNBOOK.md` + ADRs `0001` (RLS-first tenant isolation), `0002` (fail-closed, no silent local fallback), `0003` (isolated restore drills only). **Documentation only — no drill executed**; backup/recovery stays **Untested** until the runbook's §10 evidence checklist is filled in. Restore targets a NEW disposable project only: never Production, never active staging, and migration `015` stays prohibited (a drill needing schema change is a stop condition). Source branch `feature/teragon-restore-readiness` preserved at `80f6fbc`.
  - **S10.1-B EXECUTED — RESTORE DRILL PARTIAL.** Evidence: `docs/operations/evidence/RESTORE_DRILL_2026-08-03.md`. A disposable project (`teragon-restoredrill-20260803-0518a4`, ref `fftyy…myuqp`, eu-central-1) was created, reconstructed from migrations 001–014, validated and destroyed. **Headline finding: active staging has `pitr_enabled=false` and `backups: []` on a free-plan org — there is NO backup artifact to restore, so RPO is UNDEFINED/unbounded.** Native restore was therefore impossible on the facts, not on permissions; what ran was a schema+seed reconstruction, explicitly reported as PARTIAL rather than PASS. Structure matched staging exactly (47 tables, 47/47 RLS, 185 policies), isolation proofs 8/8, Customers/Contacts read paths correct with anonymous fail-closed, zero fixtures left. Backup/recovery **remains Untested** until a real backup mechanism exists (a paid-plan/billing decision, deliberately not taken) and the drill is re-run against a genuine recovery point.
  - **Superseded — was: S10.1-B (BLOCKED on human account action):** an operator with Supabase project **create + delete** rights must provision a disposable `teragon-restoredrill-<YYYYMMDD>` project and run the runbook. Cannot be automated from this session.
  - ⚠️ **CI coverage gap (historical — now fixed by S10.0-F above):** `supabase-live-validation.yml` triggers on `pull_request` with **path filters** (`supabase/**`, `src/persistence/**`, `scripts/platform/**`, `tests/persistence/**`, `tests/supabase/**`, `package*.json`). PR #6 touched none of them, so **no gate ran automatically**. Both jobs were obtained via `workflow_dispatch` (run `30830685436`, both green at `88d341a`) before merging. Any future PR outside those paths is unguarded unless dispatched manually — widening the filters is an open follow-up.
- **`customers` AND `contacts` are both `LIVE_VALIDATED`** (contacts: list read, customer-scoped read, create, update — **no delete, no `/contacts/:id`**). `/contacts` is RBAC-guarded by `customer.read`, matching its RLS SELECT policy. Every other domain remains `NOT_CONNECTED`; `AI_REMOTE_ENABLED` false; migrations **14**.
- **S10.3 demo-pilot safety MERGED (PR #11, base now `801e8e0`).** Fail-closed demo mode (`src/app/demoMode.ts`: ON unless `VITE_DEMO_MODE="false"` exactly), persistent non-dismissible Hebrew banner mounted above the router (every route incl. `/login`), and one `externalSideEffectDecision` gate blocking email/sms/webhook/push/third-party-api in demo mode. All 54 demo-seed emails normalized to `@example.com` (previously 11 `@gmail.com` + real-looking domains). `docs/operations/DEMO_PILOT_POLICY.md`: **Internal Demo Pilot ALLOWED with synthetic data; real business data PROHIBITED until B1 backup/restore closes; Production BLOCKED.** App has no email/SMS/push/webhook sender today. Source branch `feature/teragon-demo-pilot-safety` preserved at `fede405`.
- **HEAD after this session:** `65363ba` (`test(customers): validate live Supabase customer integration`). Prior: `0c9c369` (CI Node floor), `2403b2b` (preflight determinism), `e2a2201` (forensics record).
- **Tree:** clean.
- **customers status = `LIVE_VALIDATED`** — 12/12 live acceptance green against `teragon-staging` on the GitHub-hosted runner, cleanup verified, orphan audit 0. Contacts and every other domain remain `NOT_CONNECTED`. `AI_REMOTE_ENABLED` stays **false**. Migrations remain **14** (no 015).

## Authorized scope (per session — ONE bounded checkpoint)
- **S9.1-A DONE:** legacy `getRepository` hard-gate + IndexedDB boot shutdown in SUPABASE mode (commit `93611cc`).
- **S9.1-B1 DONE:** central `DomainNotConnectedGate` mounted around the page `Outlet` in `OsShell` (commit `566ce74`).
- **S9.1-B2 DONE + CHECKPOINT-A COMPLETE (this session):** shell/header wired to `useAuth` — safe identity (name · role · org, no tokens), logout via real `signOut` (routes to `/login` replace; composition returns `AUTH_REQUIRED` after — repo access invalidated). SUPABASE never shows the static `CANONICAL_USER`; LOCAL unchanged. Full Checkpoint-A gate PASS (vitest 2498/0-skipped, build, build:preview, oxlint, typecheck×2, scan:secrets CLEAN, representative Playwright 3/3).
- **S9.2-A1a DONE:** authenticated async Supabase domain seam `src/persistence/composition/loadSupabaseDomainRepository.ts` — fail-closed (provider→session→canonical identity→allow-list→lazy import), org from `identity.organizationId` (never VITE_SUPABASE_ORG/browser), typed errors (AUTH_REQUIRED / IDENTITY_INVALID / DOMAIN_NOT_CONNECTED / REMOTE_REPOSITORY_LOAD_FAILED / PROVIDER_BYPASS_FORBIDDEN), no IndexedDB/fallback. `SUPABASE_CONNECTED_DOMAINS=["customers"]` (IMPLEMENTATION_READY; NOT route-mounted / NOT in the domain map). Gate still not route-aware, so SUPABASE mode still shows the notice for all pages.
- **S9.2-A1b DONE (LIST read-connected; DETAIL deferred → PARTIAL):**
  - `src/app/data/routeDomain.ts` — route→domain map; `/customers`→`customers`, `/customers/:id`→`null` (detail deferred), else `null`. Connected decision stays central (`isSupabaseConnectedDomain`), no routing allow-list.
  - `DomainNotConnectedGate.tsx` — split into pure `DomainNotConnectedGateView({provider,pathname})` (route-aware) + `DomainNotConnectedGate` wrapper (`useLocation` + `PERSISTENCE_PROVIDER`). SUPABASE mounts a route only when its domain is connected; `/customers` renders, `/customers/:id` + all others show the notice (their IndexedDB pages never mount).
  - `src/app/data/useDomainCollection.ts` — composition read hook. LOCAL = byte-for-byte `useCollection` (shared key+queryFn → invalidation intact). SUPABASE = `enabled:authed`, `retry:false`, reads via `loadSupabaseDomainRepository`→`listSafe()`; non-ok throws typed `DomainReadError` (safe Hebrew msg, no leak); never `getRepository`, never local fallback. Query key = `["domain-collection","SUPABASE",collection,userId,orgId]` (no tokens/sessions); on loss of auth it cancels+removes cached protected rows.
  - `CustomersPage.tsx` — uses `useDomainCollection`; provider-aware loading/typed-error states; refresh; SUPABASE hides the IndexedDB create button + modal, shows Hebrew notice "יצירת לקוח עדיין אינה זמינה בסביבת התצוגה".
  - **DETAIL (`/customers/:id`) intentionally left blocked** — the 832-line 13-collection Customer-360 page is a broad rewrite (12 domains still NOT_CONNECTED); deferred to keep the commit bounded + the tree clean.
  - Tests: `tests/app/customer-read.test.tsx` (13) + rewired `tests/app/domain-not-connected-gate.test.tsx` (3). Targeted vitest 46-adjacent green, typecheck clean, oxlint clean. No full/live suite, no build:preview, no screenshots (SUPABASE list needs a live session → visual gate belongs with live validation).
  - **customers status = `READ_CONNECTED_LOCAL_TESTED`** (list only; detail NOT connected; every other domain NOT_CONNECTED).
- **S9.2-A1c DONE (customer create + update; idempotent):**
  - `src/app/data/useCustomerMutation.ts` — provider-aware WRITE seam. LOCAL = existing approved behavior (`createCustomer` / local factory update). SUPABASE = authenticated write via `loadSupabaseDomainRepository("customers",...)`, org from `identity.organizationId` only, never `getRepository`/IndexedDB, no local fallback. Typed failures: AUTH_REQUIRED / IDENTITY_INVALID / DOMAIN_NOT_CONNECTED / REMOTE_REPOSITORY_LOAD_FAILED / REMOTE_WRITE_FAILED / VALIDATION_FAILED / DUPLICATE_SUBMISSION (safe Hebrew, no raw errors).
  - **Idempotency (NO schema change):** create reuses ONE deterministic submission id + writes through `upsertSafe` — module-level in-flight collapse here + repo-level in-flight collapse + DB `onConflict:"id"` ⇒ double-click / uncertain-retry = one logical row. `entityToRow` injects the tenant `organization_id` from the canonical org (customers maps domain `organizationId`→`owning_org_id`, a different column), so a browser org can't spoof ownership. **The existing `upsertSafe` contract fully covers idempotency — no migration 015.**
  - **Session safety:** issuing identity captured; a write resolving after logout/identity change does NOT repopulate protected cache. On success, adds the verified record to the scoped `["domain-collection","SUPABASE","customers",userId,orgId]` query + invalidates only that key.
  - `CustomersPage.tsx` — create enabled in both modes (seam + submission id); new per-row "עריכה" opens a focused edit modal (essential fields only, no Customer-360); extracted `CustomerFormModal` (create+edit). Detail route still blocked.
  - Tests: `tests/app/customer-mutation.test.tsx` (10) + read regression updated. Targeted 40 green (mutation 10 + read 13 + gate 3 + loader 9 + gate 5). typecheck + oxlint clean. Visual: LOCAL dev-server a11y-inspected at 1024/1280/1440 (list + create + edit dialogs; RTL, Light, no overflow) — SUPABASE-mode visual needs live Auth (deferred to live validation).
  - **customers status = `READ_WRITE_CONNECTED_LOCAL_TESTED`** (list read + create/update; NOT LIVE_VALIDATED, NOT CRUD_COMPLETE — no remove, no contacts). Detail blocked; all other domains NOT_CONNECTED.
- **S9.2-A1d1 DONE (customer DETAIL remote read + reduced 360):**
  - `routeDomain.ts` — `/customers/:id` now maps to `customers` (single-segment regex); list + detail both mount, others blocked. Contract stays central.
  - `src/app/data/useDomainRecord.ts` — single-record read by id. SUPABASE = `enabled:authed&&hasId`, `retry:false`, `getSafe(id)` via `loadSupabaseDomainRepository`; non-ok → typed `DomainReadError`; **not-found = `null`** (TanStack forbids `undefined`). Key `["domain-record","SUPABASE",collection,id,userId,orgId]`; purge on loss of auth. Never `getRepository` in SUPABASE.
  - `src/modules/customers/SupabaseCustomerDetail.tsx` — calm reduced view: back link + name/status-chip/type header, ONE info Panel (email/phone/city/type/created/updated), ONE compact Hebrew notice "המידע המשלים יחובר בשלבי ההטמעה הבאים", edit action via `useCustomerMutation.update` (refetches the record). Mounts NO disconnected Customer-360 sections. Invalid-id / loading / not-found / safe-error states.
  - `CustomerDetailPage.tsx` — top-level branch on the build-const provider: SUPABASE→`SupabaseCustomerDetail`; LOCAL→the existing 832-line `LocalCustomerDetailPage` (unchanged). No conditional-hook hazard (const branch).
  - Tests: `tests/app/customer-detail.test.tsx` (11) + read regression updated (detail route now connected). Targeted 51 green (detail 11 + read 13 + mutation 10 + gate 3 + loader 9 + supabase-gate 5). typecheck + oxlint clean. Visual: component validated with mocked auth data (structure/RTL/responsive auto-fit → no 1024 overflow); pixel capture at 1024/1280/1440 deferred to A1d2 (needs SUPABASE build/live Auth).
  - **customers status = `READ_WRITE_DETAIL_CONNECTED_LOCAL_TESTED`** (list+detail read, create/update; NOT LIVE_VALIDATED, NOT CRUD_COMPLETE). Contacts + all other domains NOT_CONNECTED.
- **S9.2-A1d2a-1A DONE (fixture/cleanup harness, fake-client only):** `scripts/platform/live-customer-fixtures.mjs` (adapter-injected, fail-closed, `process.exit`-free core) provisions a temporary NON-admin Auth user + profile + membership in a SECOND seeded org (never org-teragon) via the Admin adapter's `createUser` (no `auth.users` SQL, no manual hashing), stamps every id/email with `accrun-<runId>`, and GUARANTEES teardown (`withCustomerFixtures` always-cleanup; cleanup failure fails the run; password held in memory, never logged). `tests/platform/live-customer-fixtures.test.ts` (9, fake in-memory Admin adapter). typecheck + oxlint clean; plan entrypoint contacts nothing. Readiness: staging seed has orgs `org-staging-demo`/`org-staging-beta` + non-admin profiles but NO loginable auth users; Actions secret `SUPABASE_SERVICE_ROLE_KEY` now configured for teragon-staging (server-side only). NO real Auth user created, NO staging mutation this checkpoint.
- **S9.2-A1d2a-1B1 DONE (real adapter + live runner READY; not executed):**
  - `scripts/platform/shared/adapters/live-customer-admin.mjs` — REAL server-only Admin adapter (injectable client): Admin API `createUser`/`deleteUser` (no `auth.users` SQL, no hashing) + PostgREST profile/membership/customer upsert/delete + `exists`. Refuses construction if a service-role key is VITE_-exposed; errors redacted (jwt/sb_/service_role).
  - `scripts/live-domains/run-domains-live.mjs` — `npm run test:domains:live`. Fail-closed `preflight` (STAGING_DOMAINS_LIVE=1, required env NAMES, provider=SUPABASE, ref=bjvirkmagwpqroakazjj, commit provenance, no VITE service_role, cleanup support) BEFORE any mutation; `STAGING_DOMAINS_DRY_RUN=1` validates the contract with ZERO createUser/db-writes/deletes; `evaluateReport` fails on skipped>0/executed=0/failed>0/cleanup!=ok.
  - `e2e/live-domains.config.ts` + `e2e/live-domains/customers.live.ts` — dedicated config (`*.live.ts`, no webServer) → **invisible to default vitest/playwright** (proven: default playwright list=0, dedicated=12). The 12-test authoritative inventory DEFINED (login→identity→CRUD→cross-org deny×2→org-override reject→pagination→idempotency→logout→remote-fail-no-IDB→cleanup), fail-closed guard, IndexedDB counters, safe `_report.json` (files/executed/passed/failed/skipped/cleanup/idb/maskedRef/commit). Password reaches only this isolated process, never the app/log.
  - `.github/workflows/customer-live-acceptance.yml` — `workflow_dispatch` ONLY; build:preview + scan:secrets run WITHOUT the service-role secret; `SUPABASE_SERVICE_ROLE_KEY` scoped to the runner step only; always uploads the redacted report.
  - `tests/platform/live-domains.test.ts` (10) + fixtures (9) = 19 green; typecheck + oxlint clean; dry-run proved zero mutations. NO real Auth user, NO staging mutation, workflow NOT triggered, no secret value read/printed.
- **S9.2-A1d2a-1B3 DONE (forensics + bounded correction):** run `30765546502` (attempt 2, sha ae49582) showed 8 failed / 4 passed — **1 direct** (test 1 admin login stuck at `/login`) + **7 cascade** (2,4,5,6,8,9,11, all `loginAdmin`-dependent); test 3 (second-org fixture login) + 7/10/12 passed. Intermittency (7/10 passed) ⇒ a login RACE, not a hard credential block. Report was spec-owned + unreliable (`executed:1`, `cleanup:not-run`, `maskedRef:"-"`).
  - **Runner** (`run-domains-live.mjs`): added server-side `adminPreflight` (anon signInWithPassword → PASS/FAIL, redacted) BEFORE Playwright — on FAIL classify `ADMIN_CREDENTIAL_MISMATCH`, no browser, no auto-password-mutation. Runner now OWNS the fixture lifecycle (provision → Playwright → **always** cleanup+verify in finally) and writes the AUTHORITATIVE report from Playwright's JSON reporter + `_idb.json`; `cleanup` is `ok|failed`, never `not-run` after setup; a cleanup failure overrides passing tests.
  - **Spec** (`customers.live.ts`): `login()` waits for the REAL Auth result (leaves `/login`, 30s budget) + trims email — fixes the race; test 3 uses runner-provisioned `ACC_FIXTURE_*`; spec emits only `_idb.json`.
  - **Fixtures** (`live-customer-fixtures.mjs`): cleanup now VERIFIES deletion via `adapter.exists` (ok only when the row is gone).
  - Tests: `tests/platform/live-domains.test.ts` (+10 → 29 total with fixtures) — preflight pass/fail/missing-env/redaction, PW-JSON totals, cleanup-failure ⇒ FAIL, cleanup-not-run defensive fail, verified-deletion. typecheck + oxlint clean; dry-run 0 mutations; default-suite non-discovery intact. **Orphan audit: 0 accrun- leftovers.**
- **S9.2-A1d2a-1B4 (credential reconciliation):** root cause of run 30768533361 = stale GitHub secret VALUES (local `.env.staging.local` creds verified PASS: ref/admin-found/email-confirmed/password-auth all yes). Synced 4 repo secrets (`SUPABASE_URL`, `SUPABASE_ANON_KEY`, `TERAGON_ADMIN_EMAIL`, `TERAGON_ADMIN_PASSWORD`) from local via `gh secret set` (stdin, no values printed; service-role untouched). Added a NON-mutating GitHub-hosted preflight: runner `fullPreflight` (service-role find/confirm/access + anon password auth) + `STAGING_DOMAINS_PREFLIGHT_ONLY=1` mode + workflow `preflight_only` input. Workflow→runner env mapping confirmed correct (5 names via `secrets.*`, no misspelling/PUBLISHABLE/empty fallback).
- **S9.2-A1d2a (github preflight forensics):** added safe `diagnose()` (structural/key-class/network, no secret content) + `normalizeCred()` (strips whitespace from URL/anon/service keys at every createClient site; password NEVER touched) — commit `d47013f`. CI preflight run `30805217627` proved: URL/SERVICE/EMAIL/PASSWORD GitHub secrets are newline-contaminated (`hasNewline`); `authSettingsStatus=200` (URL+anon valid); but SERVICE_ROLE + PASSWORD are rejected **even after normalization** → the operator's UI-pasted values for those two are WRONG/old (valid JWT claims, invalid signature), not merely whitespace. `.env.staging.local` is clean + `fullPreflight` all-true locally.
  - ⚠️ **That "wrong/old secret values" conclusion was WRONG and is superseded by S9.2-A1d2a-1B2 below.** The service-role key and admin password were valid all along; the real blocker was the runner's Node version. Recorded here so the mistaken inference is not re-derived.
- **S9.2-A1d2a-1B2 DONE — CUSTOMER LIVE ACCEPTANCE PASS.** Validated SHA `65363ba0f74cfa91998d7b8733d002728ad485ee`.
  - **Root cause (proven, not inferred):** `@supabase/supabase-js@2.111.0` declares `engines: { node: ">=22.0.0" }`, but `customer-live-acceptance.yml` pinned `node-version: 20`. `npm ci` does **not** enforce engines, so the runner installed cleanly and only failed at call time *inside* the SDK. Every check routed through the SDK client failed while the raw `fetch` to `/auth/v1/settings` returned **200** — so the report read `serviceRole=fail:auth_error passwordAuth=fail:auth_error` and was indistinguishable from bad credentials. `fail:auth_error` is only reachable when `error.status` is undefined, i.e. it was never an `AuthApiError`; a real rejection would have shown `fail:400`. Control: identical exported `fullPreflight`/`diagnose` run locally on Node 25 with the same values → all five booleans PASS.
  - **Secret contamination was real but NOT the blocker.** Run `30806206099` showed `hasNewline`+`leadingTrailingWs` on 4 of 5 secrets (ANON alone clean). All five were re-synced from `.env.staging.local` — parsed with the project's own `parseEnvText`, normalized for URL/anon/service/email only (**password preserved byte-exact**), and written by a throwaway Node helper invoking `gh secret set` **once per secret with the value on stdin only** (`spawnSync`, `shell:false`, no argv, no shell interpolation, no temp file, gh's stdout/stderr discarded, no trailing newline). Helper deleted after use. Run `30807115015` then showed `struct` all-false with the SAME auth failure — which is what isolated the cause to the runtime. No Environment-scoped secrets exist (staging env empty) and the repo is under a personal account (org-secrets API → 422), so no shadowing.
  - **Two further defects the suite had never reached** (every prior run died at the credential stage, so the 12 browser tests had never executed): (1) the runner passed `--reporter=list,json`, and a CLI reporter list REPLACES the config's, after which `PLAYWRIGHT_JSON_OUTPUT_NAME` resolved **relative to the config dir** → the report landed at `e2e/e2e/live-domains/_pw.json` while the runner read `e2e/live-domains/_pw.json`, so totals were always `executed=0` ("zero tests executed") even when all 12 passed; the config now owns list+json with an `outputFile` resolved from `process.cwd()`. (2) tests 1–2 asserted text the shell never renders that way — `getByText(/טרגון|Teragon/)` matched **both** header spans (strict-mode violation) and `/טרגון/` matched **none** (the org renders Latin "Teragon"); both now assert the specific `.os-header__name` / `.os-header__role` elements. Assertions were **narrowed, never weakened**.
  - **Workflow hardening:** a checkout provenance gate prints requested / actual (`git rev-parse HEAD`) / workflow-definition SHA and fails on mismatch (run #9's logs could not evidence the real checkout SHA at all); Playwright install + `serve dist` are now gated behind `!preflight_only` so a preflight is headless and non-mutating by contract; `runtime.nodeMajor` vs the SDK floor and a redacted `safeErrorName` (error CLASS only, letters-stripped) are reported so a runtime fault can never again masquerade as a credential fault.
  - **Evidence:** preflight run **`30809789015`** → `targetProjectVerified=yes serviceRoleAdminAccess=pass adminUserFound=yes emailConfirmed=yes adminPasswordAuth=pass verdict=PASS`, `nodeMajor=22`, `struct` all-false, bundle secret scan CLEAN, no Playwright, no fixtures. Full run **`30809944248`** → `executed=12 passed=12 failed=0 skipped=0 cleanup=ok idb=0/0/0`, `PASS — all required live customer checks passed + cleanup verified`. Both at checkout SHA `65363ba`, provenance gate green.
  - **Cleanup / orphan audit:** non-mutating read-only audit (GoTrue admin list + PostgREST GETs) over `auth.users`, `profiles` (by `email` and `name` — `profiles.id` is a uuid, so an `accrun-` LIKE scan is invalid: PG `42883`), `memberships`, `customers`, `organizations` → **ORPHAN_TOTAL=0**, against populated tables (5/5/5/3), so the zero is real coverage and not an empty-permission artifact.
  - **Untouched:** Production, deployment, merges, migration 015, schema/RLS/storage/functions, the Supabase admin password, the staging administrator user. No secret value, length, prefix, suffix or hash was ever printed.
- **Next:** no other domains, no migration 015. `customers` is the only LIVE_VALIDATED domain.
- **S9.3-A DONE (contacts MAP ONLY — nothing connected, no production code):** branch `feature/teragon-supabase-contacts-domain` cut from `feature/teragon-supabase-app-auth` @ `c8a10c2`.
  - **Status at the time of the map (superseded by S9.3-E below — contacts is now LIVE_VALIDATED):** `contacts` was `NOT_CONNECTED`, absent from `SUPABASE_CONNECTED_DOMAINS` and `routeDomain.ts`, with **no Contacts route or page** — the only Contact UI is the read-only contacts section inside the LOCAL `CustomerDetailPage` (`useCollection<Contact>("contacts")` filtered by `customerId`), which never mounts in SUPABASE mode. Beware: most `contact*` hits in `src/` are `contactState`, a **Customer** field, not the Contact entity.
  - **Schema is SUFFICIENT — NO migration needed (and 015 stays forbidden).** `003_crm.sql` defines `contacts(id, organization_id NOT NULL → organizations, customer_id NOT NULL → customers, name, role, phone, email, is_primary, created_at, updated_at)` + `set_updated_at` trigger + RLS enabled; `010_indexes.sql` indexes `organization_id` and `customer_id`; `012_rls_policies.sql` grants select=`customer.read`, insert/update/delete=`customer.update`. Migrations remain **14**.
  - **Persistence is ALREADY complete:** `src/persistence/supabase/domains/crm.ts` maps `contacts` (idPrefix `ct`, `contactSchema`, all six fields). `tenant` defaults true, so the mapper injects `organization_id` and `SupabaseRepository` filters `.eq("organization_id", …)`. **Simpler than customers:** `Contact` has no domain `organizationId`, so there is no `owning_org_id` split — the tenant org is the only org. `tests/supabase/liveAdapter.test.ts` already creates a contact against a live ephemeral DB, so mapping + RLS are proven.
  - **Therefore the remaining work is composition + UI only** — the same shape customers took in S9.2-A1b/A1c/A1d1.
- **S9.3-B/C/D DONE (local):** `/contacts` list read; customer-scoped contacts section on the SUPABASE customer detail; contact create + update via `useContactMutation` (deterministic-id `upsertSafe` idempotency, session-safe cache, `ContactInput` carries no customerId/org so a contact cannot be re-parented). Branch `feature/teragon-supabase-contacts-domain`, Draft PR **#5** → base `feature/teragon-supabase-app-auth`.
- **S9.3-E DONE — CONTACTS LIVE VALIDATED.** Validated SHA `1f91d1f4e71aaa715d7883072024ce9f150a5078`.
  - **contacts status = `LIVE_VALIDATED`** (list read, customer-scoped read, create, update). **No delete, no `/contacts/:id`.** `customers` remains `LIVE_VALIDATED`. Every other domain stays `NOT_CONNECTED`. `AI_REMOTE_ENABLED` still **false**. Migrations remain **14** (no 015).
  - **Infrastructure reused, not duplicated:** `ACC_SUITE` selects ONE `*.live.ts` suite so each run reports exactly its own inventory (unset = original all-match); the workflow gained a `suite` choice input and derives `ACC_CONTACT_COUNT` from it, so the **customers run's fixture footprint is unchanged**.
  - **Fixture placement (non-obvious):** the browser signs in as the administrator and RLS correctly hides the second organization, so contact fixtures must live in the **PRIMARY** org — one temporary `accrun-…-pcust` customer + 2 contacts. The second-org user/customers are untouched and are what prove isolation (test 11).
  - **Cleanup (the subtle part):** contacts are deleted **before** their parent customer (FK order) **and swept BY PARENT** via `deleteContactsForCustomer`, because the create test mints an app-generated `ct-<uuid>` id the fixture handle can never know. Verified with `countBy`, so "cleanup ok" means the rows are actually gone.
  - **Evidence:** preflight run **`30821993510`** → all five booleans PASS (`nodeMajor=22`, `struct` all-false, bundle scan CLEAN, no Playwright, no fixtures). Full run **`30822207180`** → `executed=12 passed=12 failed=0 skipped=0 cleanup=ok idb=0/0/0`. Both at checkout SHA `1f91d1f`, provenance gate green.
  - **Orphan audit ORPHAN_TOTAL=0** across `auth.users`, `profiles` (by email+name — `profiles.id` is a uuid so a LIKE scan is invalid, PG `42883`), `memberships`, `organizations`, `customers`, and `contacts` **by id AND by parent customer**.
  - **Local pre-flight of the change:** contacts 12/12, customers suite re-run 12/12 (no regression from the shared-infra edit), unit tests 122/122, typecheck + typecheck:tests clean, scoped oxlint clean.

## Frozen facts (recorded once — do NOT re-verify)
- Backend fully stood up on live `teragon-staging` (`bjvirkmagwpqroakazjj`): 14 migrations, schema verified, RLS 8/8, admin `soundcloudillusion@gmail.com` bootstrapped, deterministic seed. **S7.1/S7.2 PASS.**
- **S8 Supabase Auth is live-validated** (`test:auth:live` 8/8). Browser key = publishable; server key stays server-only.
- **Netlify Preview env** configured via committed `netlify.toml [context.deploy-preview.environment]` (public values only). **S7.3A PASS.**
- **Trusted-AI Tested-MVP** package exists (`evals/`, `docs/trusted-ai/`). Verdict: platform=**FIX**, AI=**NOT YET EVALUATED**. `TA-B1` (silent IndexedDB) and `TA-H5` (approval workflow) are the open cards.
- App browser env contract var: `VITE_SUPABASE_ANON_KEY` (holds the publishable key), read in `src/persistence/supabase/client.ts`.
- Composition foundation (`d799f13`): `src/persistence/composition/{domainComposition.ts, DomainRepositoryProvider.tsx, DomainNotConnectedGate.tsx}` — pure fail-closed boundary, `SUPABASE_CONNECTED_DOMAINS = []` in Checkpoint A, `useDomainRepository`, central not-connected gate + safe `ProviderDiagnostic`.

## PR #3 — MERGED into `feature/teragon-supabase-app-auth` (was: merge-readiness audit at `524f0db`)
- Mergeable, **no conflicts**; base `feature/teragon-supabase-app-auth`; still **Draft**; no reviews, zero unresolved review threads; diff = 39 files, all in S9 scope; no env/secret file tracked (`.env.staging.local` untracked; only the *scanner* `scripts/scan-bundle-secrets.mjs` matches "secret").
- ✅ **`Static application gate (no Supabase)` is GREEN at `5183c6b`** (both PR checks pass). It had been RED on **every** prior commit of the branch (`d47013f`, `e2a2201`, …) — introduced by `2b54cfe` / `7f5a93a` / `e5d5c17` / `372a7b1`, never by the live-acceptance work.
- **Fix (`5183c6b`, tests only):** `typecheck:tests` went 42 → **0**. Root cause: `vi.fn` takes **one** function-type generic in Vitest 4, but these mocks used the legacy `<Args, Return>` pair, which resolved to `never` and cascaded into the TS2345/TS2493/TS2352 failures. Corrections: single-generic `vi.fn<(a: A) => R>()`; `loadRepo` mirrors `loadSupabaseDomainRepository(collection, ctx)` so `mock.calls` is a real tuple (the `{ identity }` casts became redundant and were removed); `INPUT` typed as the exported `CustomerInput` so `type` keeps its literal union; `fakeCreate` mirrors `createSupabaseRepository`'s parameters; the `.mjs` fixture-harness body param annotated to the one field it reads. No `any`, no `@ts-ignore`/`@ts-expect-error`, no weakened assertions, **no production code touched** — so the live-acceptance evidence still stands.
- Live evidence still valid at HEAD: `65363ba..524f0db` is docs + a **comment-only** edit to `domainComposition.ts` — zero behavior change, so preflight `30809789015` and full run `30809944248` still describe this tree.

## Current blocker / gating
- **Migration 015 (`approve_and_create_task` RPC) is NOT authorized** — required later for S9.2.B (approval→task atomicity), excluded from S9.x.A. Do not create it.
- Domain data layer not yet consumed by UI (root of `TA-B1`) — being wired incrementally: composition (d799f13) → hard-gate + boot shutdown (this commit) → wrapper + shell (S9.1-B) → CRM (S9.2.A).

## Files permitted next session (S9.1-B) — targeted, no repo-wide scan
- `src/persistence/composition/DomainNotConnectedGate.tsx` (already drafted — mount it)
- The app shell/header (e.g. `src/**/OsShell*`, header component) — read via known paths, not a scan
- `src/auth/useAuth.ts` / `authContext.ts` (S8 context, read-only)
- App router where the wrapper mounts (`src/app/router*`)
- New/updated targeted test file(s)

## Commands permitted (ladder)
- L1/L2 (implementing): `npx vitest run <targeted files>`; `npm run typecheck`; `npx oxlint <changed paths>`.
- L3 (Checkpoint-A gate, ONCE, at end of S9.1-B): full `npx vitest run`, `npm run build`, secret + privileged-bundle scans, ordinary Playwright, a11y.
- **Never** run full suite / build / live commands per edit.

## Prohibited (all sessions until explicitly authorized)
- Create migration 015 / any staging schema change; connect CRM or other domains before their session; enable AI/Graph flags; deploy Netlify; merge any PR; create an RC; deploy Production; restore `stash@{0}`; launch background subagents for precision-critical implementation.

## Safe resume point
- This S9.1-A commit (clean tree). If interrupted, resume S9.1-B from here.

## Invariants (assert only if a command contradicts)
- Exactly 14 migrations; `015` absent. PR #1/#2/#3 draft/unmerged. `LOCAL_INDEXEDDB` default outside Preview. Graph/AI flags OFF. Phase-11 prototype disabled. `stash@{0}` untouched.

## Expected final return (each session)
Numbered, ≤500 words: reconciled HEAD → commit → changed files → behavior implemented → targeted tests executed (passed/failed) → confirm no full/live suite ran → invariants intact → verdict (PASS / PARTIAL — SAFE RESUME REQUIRED / FAIL) → exact next checkpoint.
